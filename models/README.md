# Model files

- `yolov8n.pt`: Ultralytics YOLOv8n weights used for general vehicle detection.
- `license_plate_detector.pt`: custom YOLO weights used to locate license plates before OCR.

The custom model's training dataset, label schema, and training configuration were not included in the supplied project files. Its provenance and validation therefore cannot be independently reproduced from this repository alone.

